#include <iostream>

using namespace std;

int main() {
    string s;
    while (cin >> s) {
        for (int i=0; i<name.length(); i++) cout << name[name.length()-i-1];
        cout << endl;
    }
}
