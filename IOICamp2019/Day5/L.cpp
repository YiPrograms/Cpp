#include <bits/stdc++.h>
using namespace std;

int a[100005], dp[100005];
bool used[100005];

int main() {
    int n;
    cin >> n;
    for (int i=0; i<n; i++) cin >> a[i];
    do {
        for (int i=0; i<n; i++) {
            if (used[i]) continue;
        }
    }
}

