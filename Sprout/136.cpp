#include <bits/stdc++.h>
using namespace std;

void Report(int id) {
    cout << "Report " << id << "\n";
}

bool dp[]

void solve(int N,long long K,long long A[]) {
    
}

int main() {
    int n, k, a[45];
    cin >> n >> k;
    for (int i=0; i<n; i++) cin >> a[i];
    solve(n, k, a);
}
