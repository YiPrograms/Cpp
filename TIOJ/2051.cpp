#include <bits/stdc++.h>
using namespace std;

vector<string> names;

vector<int, int> solve() {
    char c;
    if (cin >> c) {
        if (c>=''A' && c<='Z') {

        }
    }
}

int main() {
    solve();
}
