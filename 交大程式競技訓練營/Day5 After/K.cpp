#include <bits/stdc++.h>
using namespace std;

int a[15];

int main() {
    int n;
    cin >> n;
    cin.get();
    while (n--) {
        string s;
        getline(cin, s);
        stringstream ss(s);
        int n=0;
        while (cin >> a[n++]);
        for (int i=0; i<=n; i++) {
            for (int j=0; j<=n-i; j++) {

            }
        }
    }
}
